#include <stdio.h>
#include <math.h>

double discriminante(double a, double b, double c)
{
    double discriminante;
    discriminante= b*b - 4*a*c;
    return discriminante;
}

double sqrt(risultato)
{
    double risultato;
    radice = sqrt(risultato);
    printf("La radice quadrata di %lf è %lf", risultato, radice);
    return 0;

}

double main()
{
    double risultato= discriminante(2, 8, 4);
     printf("%lf\n", risultato);

     if(risultato>0)
     


    
   
}