#include <stdio.h>

int main()
{
    int x;
    int y;
    printf("inserisci un valore x\n");
    scanf("%d\n", &x);
    printf("inserisci un valore y\n");
    scanf("%d\n", &y);
    if(x>0 && y>0)
    {
        printf("le coordinate si trovano le primo quadrante");
    }
    else if(x<0 && y>0)
    {
        
    }



}